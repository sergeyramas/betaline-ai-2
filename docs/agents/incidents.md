# Incidents / постмортемы — betaline-ai-2

Формат: дата · симптом · причина · фикс · ссылка.

- **2026-08-25 · Actions-раны падали мгновенно с 0 джобов, dispatch «no workflow_dispatch trigger»** · причина: невалидный YAML — двоеточие со пробелом внутри неквотированного `echo "### Задеплоено: …"` в deploy-vercel.yml; GitHub регистрировал workflow по пути файла и валил каждый запуск на старте · фикс: block-scalar `run: |` и убрано двоеточие · commit после b4f8466. Урок: «failure с jobs:[]» — это парсер, а не секреты.
- **2026-08-25 · Полностраничные скриншоты показывали пустые секции ниже фолда** · причина: reveal `.rv` через IntersectionObserver + гонка при программном скролле; страдали и печать/боты/якорные прыжки · фикс: failsafe в main.js — через 5 с после load все `.rv` принудительно `.visible`, observer отключается · не удалять при рефакторинге main.js.
- **2026-08-19..25 · Наследие боевого lead.js (betaline-landing)** · поле `plan` из тарифной модалки не деструктурировалось — тариф терялся во всех 4 каналах; `sendEmail` падал на `source:'pricing'` (нет ветки в linesBySource); модалка показывала успех в `.catch` · в этом репо исправлено (api/lead.js); на боевом репо живо до переезда v3.
- **Открытый риск (боевой репо betaline-landing) · утёкший Telegram bot-токен** в `08_audit_form.html`, файл деплоится статикой и публично доступен · при переезде v3: добавить файл в .vercelignore боевого, токен отозвать через BotFather, обновить `TELEGRAM_BOT_TOKEN` в Vercel env и на VPS бота.
